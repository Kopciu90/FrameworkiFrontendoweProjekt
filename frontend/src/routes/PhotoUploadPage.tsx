import React from "react";
import UploadForm from "../components/PhotoPosts/UploadForm";

const PhotoUploadPage = () => {
    return (
        <>
            <UploadForm />
        </>
    );
};

export default PhotoUploadPage;
